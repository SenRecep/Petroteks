@model (DynamicPage Ins, string Value, int Power)



<li>

    <div class="media">
        <a asp-area="" asp-controller="Home" asp-action="DynamicPageView" asp-route-pageName="@Model.Ins.Name" asp-route-id="@Model.Ins.id">
            <div href="#" class="pull-left">
                <i class="circle icofont-street-view"></i>
            </div>
            <div class="media-body">
                <span class="media-meta pull-right">@Model.Power Eşleşme</span>
                <h4 class="title">
                    @Model.Ins.Title
                    <span class="pull-right pagado">(Özel Sayfa)</span>
                </h4>
                <p class="summary"> @Model.Value</p>
            </div>
        </a>
    </div>

</li>